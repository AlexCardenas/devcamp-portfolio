<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @blog.body %>
</p>

<%= link_to 'Edit' if logged_in?(:site_admin), edit_blog_path(@blog) %> |
<%= link_to 'Back', blogs_path %>
